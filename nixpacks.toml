# Nixpacks configuration for Railway

# Use Python and Node providers
providers = ["python", "node"]

[phases.build]
cmds = [
    "cd frontend && npm install && npm run build",
    "cp -r frontend/dist/* ./"
]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-8080}"

